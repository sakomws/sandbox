apiVersion: v1
kind: Service
metadata:
  name: {{ include "downscaler.fullname" . }}
  labels:
    app: {{ include "downscaler.name" . }}
    chart: {{ include "downscaler.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "downscaler.name" . }}
    release: {{ .Release.Name }}
